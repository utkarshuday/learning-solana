[toolchain]
package_manager = "pnpm"

[features]
resolution = true
skip-lint = false

[programs.localnet]
staking = "3vtpaGR9RyXS8QCpkGB9AvtDCmhEAUFZsH7joVSA2mjx"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "pnpm codama run js; pnpm tsx --test --test-reporter=spec tests/*.test.ts"
generate-client = "anchor build; pnpm codama run js"
